{
  "name": "label-prestige-fork2",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "GITHUB_ACTIONS=true npm run build && npx gh-pages -d dist -m 'Deploy: $(date)'"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^8.0.16"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.2",
    "firebase": "^12.14.0",
    "three": "^0.184.0"
  }
}
